#include<iostream>
#include<fstream>
#include<string>
#include<set>

#include "Task.h"
#include "TaskSet.h"
#include "EDF_Algorithm.h"

/**
 * Runs the Earliest Deadline First (EDF) algorithm on the given task set.
 *
 * @param taskSet The task set to be scheduled using EDF algorithm.
 * input: TaskSet include "m_numTasks" task 
 * output: print "finish the assignment" and the occurring time 
 * exception: print "fail process management"
 */
void EDF_Algorithm::runEDF(TaskSet taskSet)
{
    


    printf("finish the assignment");    
}

int EDF_Algorithm::changePriorityLevel(TaskSet taskSet)
{
    // change the priority level of the task set
    
    return 0;
}
